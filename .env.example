# ── Segredo JWT ────────────────────────────────────────────────────────────────
# Gere um valor seguro com: openssl rand -hex 48
# Copie este arquivo para .env e preencha os valores reais.
JWT_SECRET=substitua_por_um_segredo_forte_gerado_com_openssl
